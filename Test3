local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

-- Создание графического интерфейса
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = CoreGui
ScreenGui.Name = "MM2LoopHackGui"

local ToggleButton = Instance.new("TextButton")
ToggleButton.Parent = ScreenGui
ToggleButton.Size = UDim2.new(0, 180, 0, 50)
ToggleButton.Position = UDim2.new(0.05, 0, 0.4, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Font = Enum.Font.SourceSansBold
ToggleButton.TextSize = 14
ToggleButton.Text = "Режим: ОБЫЧНЫЙ"
ToggleButton.Active = true
ToggleButton.Draggable = true

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = ToggleButton

-- Переменные состояний (0 = обычный, 1 = бессмертие, 2 = невидимость)
local currentMode = 0
local clone = nil
local loopConnection = nil
local godConnection = nil

-- Функция для полной очистки всех режимов
local function resetAll()
    if loopConnection then loopConnection:Disconnect(); loopConnection = nil end
    if godConnection then godConnection:Disconnect(); godConnection = nil end
    
    player.Character = character
    workspace.CurrentCamera.CameraSubject = character:FindFirstChild("Humanoid")
    
    if character and character:FindFirstChild("HumanoidRootPart") and clone and clone:FindFirstChild("HumanoidRootPart") then
        character.HumanoidRootPart.CFrame = clone.HumanoidRootPart.CFrame
    end
    
    if clone then
        clone:Destroy()
        clone = nil
    end
end

ToggleButton.MouseButton1Click:Connect(function()
    character = player.Character or player.CharacterAdded:Wait()
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end
    
    -- Переключаем режим по кругу (0 -> 1 -> 2 -> 0)
    currentMode = (currentMode + 1) % 3
    
    if currentMode == 1 then
        -- === РЕЖИМ 1: БЕССМЕРТИЕ ===
        resetAll() -- на всякий случай чистим старое
        ToggleButton.Text = "Режим: БЕССМЕРТИЕ"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(50, 150, 50) -- Зеленый
        
        -- Годмод: удаляем триггеры урона у всех чужих ножей каждую секунду
        godConnection = RunService.Stepped:Connect(function()
            for _, plr in pairs(Players:GetPlayers()) do
                if plr ~= player and plr.Character and plr.Character:FindFirstChild("Knife") then
                    local knife = plr.Character.Knife
                    if knife:FindFirstChild("TouchInterest") then
                        knife.TouchInterest:Destroy()
                    end
                end
            end
        end)
        
    elseif currentMode == 2 then
        -- === РЕЖИМ 2: НЕВИДИМОСТЬ ===
        resetAll()
        ToggleButton.Text = "Режим: НЕВИДИМОСТЬ"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50) -- Красный
        
        -- Создаем локального полупрозрачного клона
        character.Archivable = true
        clone = character:Clone()
        clone.Parent = workspace
        
        for _, part in pairs(clone:GetDescendants()) do
            if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                part.Transparency = 0.5
                part.CanCollide = false
            elseif part:IsA("Decal") then
                part.Transparency = 0.6
            end
        end
        
        workspace.CurrentCamera.CameraSubject = clone:FindFirstChild("Humanoid")
        player.Character = clone
        
        -- Настоящее тело прячется под карту, пока вы бегаете клоном
        loopConnection = RunService.Heartbeat:Connect(function()
            if character and character:FindFirstChild("HumanoidRootPart") and clone and clone:FindFirstChild("HumanoidRootPart") then
                character.HumanoidRootPart.CFrame = clone.HumanoidRootPart.CFrame * CFrame.new(0, -150, 0)
                character.HumanoidRootPart.Velocity = Vector3.new(0, 0, 0)
            end
        end)
        
    else
        -- === РЕЖИМ 0: ОБЫЧНЫЙ ===
        resetAll()
        ToggleButton.Text = "Режим: ОБЫЧНЫЙ"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35) -- Серый
    end
end)
