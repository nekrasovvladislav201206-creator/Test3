local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

-- Создание UI кнопки
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = CoreGui
ScreenGui.Name = "MM2InvisSimpleGui"

local ToggleButton = Instance.new("TextButton")
ToggleButton.Parent = ScreenGui
ToggleButton.Size = UDim2.new(0, 160, 0, 50)
ToggleButton.Position = UDim2.new(0.05, 0, 0.4, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Font = Enum.Font.SourceSansBold
ToggleButton.TextSize = 16
ToggleButton.Text = "MM2 Инвиз"
ToggleButton.Active = true
ToggleButton.Draggable = true

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = ToggleButton

local isInvis = false
local clone = nil
local loopConn = nil

ToggleButton.MouseButton1Click:Connect(function()
    character = player.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end
    
    isInvis = not isInvis
    
    if isInvis then
        ToggleButton.Text = "Выключить инвиз"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        
        -- Клонируем персонажа для себя
        character.Archivable = true
        clone = character:Clone()
        clone.Parent = workspace
        
        -- Делаем клона полупрозрачным
        for _, part in pairs(clone:GetDescendants()) do
            if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                part.Transparency = 0.5
                part.CanCollide = false
            elseif part:IsA("Decal") then
                part.Transparency = 0.6
            end
        end
        
        -- Прячем оригинал от других игроков (опускаем под текстуры), но оставляем хитбокс для предметов
        loopConn = RunService.Heartbeat:Connect(function()
            if isInvis and character and character:FindFirstChild("HumanoidRootPart") and clone and clone:FindFirstChild("HumanoidRootPart") then
                local realRoot = character.HumanoidRootPart
                local cloneRoot = clone.HumanoidRootPart
                
                -- Сервер видит вас внизу, но из-за отсутствия античита вы можете использовать ножи/предметы
                realRoot.CFrame = cloneRoot.CFrame * CFrame.new(0, -250, 0)
                realRoot.Velocity = Vector3.new(0, 0, 0)
            end
        end)
        
        -- Переключаем камеру и управление на прозрачного клона
        workspace.CurrentCamera.CameraSubject = clone:FindFirstChild("Humanoid")
        player.Character = clone
        
    else
        -- Выключение без смерти и ресета
        ToggleButton.Text = "MM2 Инвиз"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        
        if loopConn then loopConn:Disconnect() end
        
        player.Character = character
        workspace.CurrentCamera.CameraSubject = character:FindFirstChild("Humanoid")
        
        if character and character:FindFirstChild("HumanoidRootPart") and clone and clone:FindFirstChild("HumanoidRootPart") then
            -- Возвращаем оригинал на место клона живым
            character.HumanoidRootPart.CFrame = clone.HumanoidRootPart.CFrame
        end
        
        if clone then
            clone:Destroy()
            clone = nil
        end
    end
end)
